import { ChatTextNamePipe } from './chat-text-name.pipe';

describe('ChatTextNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ChatTextNamePipe();
    expect(pipe).toBeTruthy();
  });
});
